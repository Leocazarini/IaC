# Modulo de borda publica na Cloudflare.
#
# Cobre DNS, proxy, modo de TLS e regras de WAF. Consome do modulo de compute
# apenas o endereco publico; nao conhece o provedor onde a origem roda.

# Registros DNS, configuracao de zona e regras de WAF
# (implementacao na proxima fase)
